# 工作记录

## 2026-09-22：本地 Git 与上下文初始化

- 用户授权将目录转为 Git 项目并维护上下文；指定主文档、mip0 和 mip6 用例正确。重复的 mip0 名称按同一文件处理。
- 保存全部 20 个既有文件至原始提交 `405c58b441e970b1043ba2c118bf7230b130e79f`，之后单独整理上下文。保留 backup 和历史分析。
- 重写根目录与 analysis 目录 README，增加 AGENTS.md、项目上下文、问题队列、可信文件清单、基准校验脚本和 Git 忽略/字节保留规则。
- 观察：可信文件的源版本需要区分；mip6 源哈希匹配备份；旧 README 存在缺失文件链接。详见上下文及 R01/R07。
- 本轮为版本管理与上下文整理，未执行历史算术脚本、未复算算法、未联网核验上游，也未改变用户指定的可信状态。
- 提交使用命令级身份 Codex <codex@localhost>，未写全局身份配置。沙箱账户与目录所有者不同，工具执行 Git 时使用仅对当前命令有效的 safe.directory 设置，未放宽全局安全配置。

实际验证：

- 可信基准脚本：3/3 文件 SHA-256 与长度一致。
- 初始目录快照对比：除两个导航 README 外，其余 18/18 个原有文件字节不变。
- 两个导航 README：24 个本地链接均有效。
- git diff --check 通过。
- 系统默认禁用 PowerShell 脚本，文档命令使用进程级 -ExecutionPolicy Bypass；未修改系统执行策略。

## 2026-09-22：用例集中与历史资料归档

- 根据用户要求，将 mip0、mip4、mip6 三个逐步用例原样移至 case/。
- 将其余 analysis 资料归档至 backup/analysis/；原 upstream 与 verilog 仅含历史说明，一并归档。旧源备份集中到 backup/source_snapshots/。
- 旧复核队列移至 backup/context/REVIEW_QUEUE.initial.md。备份默认不读取、不搜索、不作为当前依据，也不安排主动复核；用户认为后续很可能不会用到。
- 更新 README、AGENTS、当前上下文、用例索引与基准路径。新增 backup/README.md 和 backup/AGENTS.md，明确历史声明不覆盖当前规则。
- 用户解释源哈希不同可能因为产生于不同对话。作为来源背景保留，不据此降低可信状态，也不将历史版本追溯列为默认前置工作。
- 已询问“3 个可信用例”是否包含 mip4；截至本轮提交尚无回答，mip4 暂保留待确认，原先明确确认的主文档、mip0、mip6 状态不变。目录迁移已全部完成。
- 验证：已确认文件 3/3 哈希及长度通过；相对迁移前 Git HEAD，20 个研究/归档文件的 Git blob 完全一致；新入口的 11 个本地链接有效；git diff --check 通过。
- 本轮没有技术正文修改、算法复算或上游验证。

## 2026-09-22：确认 mip4 并统一用例编号

- 用户明确确认 mip4 可信；可信基准现为主文档和三个用例，共 4 个文件。此前记录的 mip4 待确认状态已在当前上下文中解除。
- 按用户指定重命名：mip4 → case/case0_mip4_xyz_5_6_3.md；mip0 → case/case1_mip0_xyz_69_134_195.md；mip6 → case/case3_mip6_xyz_1_1_17.md。去除 step_by_step 后缀。
- 命名格式为 case{num}_mip{level}_xyz_{x}_{y}_{z}.md；保留 case2 空缺，下一例使用 case4，以后按已分配最大编号加 1 递增，不重排、不复用。规则集中在 case/README.md，并写入 AGENTS.md 与当前上下文。
- 更新可信清单中的路径，并为新获确认的 mip4 记录哈希和长度；基准校验脚本改为从清单读取文件数量，以支持未来扩展。
- 验证：三个重命名用例与迁移前 Git blob 完全一致；可信文件 4/4 哈希及长度校验通过；根目录与 case 索引共 10 个本地链接均有效。未复算算法，未读取或修改 backup 内容。
