# 项目上下文

最后更新：2026-09-22。

## 当前目标与状态

用户要求将现有目录变成 Git 项目并管理上下文，同时明确某些旧文件可能有错。本轮建立本地 Git、保存原始快照、整理入口与信任分级；尚未开展全面算法审计。

工作目录：D:/project/no_preject/addrlib。本地分支：main。原始快照：`405c58b441e970b1043ba2c118bf7230b130e79f`，包含全部 20 个既有文件。未配置远程。初始化提交使用 Codex <codex@localhost> 标注工具生成的提交，没有修改全局 Git 身份配置。

## 用户确认的事实

以下 3 个唯一文件由用户明确确认正确，保留原文：

| 文件 | 基准用途 |
| --- | --- |
| ADDRLIB.md | 当前主文档 |
| analysis/case_mip0_xyz_69_134_195_step_by_step.md | mip0 用例；文中结果 0x31440175 |
| analysis/case_mip6_xyz_1_1_17_step_by_step.md | mip6 用例；文中结果 0x10160A2C |

结果为可信文档中的记录，本轮没有重新执行算法复算。用户把 mip0 文件列了两次，未将 mip4 列入可信范围。其他既有技术资料默认待核实，不等于已确认错误。

实际 SHA-256 和长度见 TRUSTED_BASELINE.json。可信状态依据为用户在 2026-09-22 的明确指示，而非本轮技术验证。

## 当前观察到的版本关系

- 当前 ADDRLIB.md SHA-256 为 `537CFA6D9FAFBAF3601B219B686D94CAA4B7C69FDFE89005DFF36A0B4CB6D3BB`。
- mip0 引用源哈希 `216C030D9A6F2CF1E3AE88039F0C566A3484B16026FB496DFFEAFA4D0253E73B`，与当前主文档不一致。
- mip6 引用源哈希 `05D4519FBF57BB516590AE397DD67EDB2FE6FF7A09570325D033A3D73702216D`，与当前主文档不一致，但与 backup/ADDRLIB.md 实际哈希一致。匹配不意味着该备份已获用户单独确认。
- 哈希差异只能证明字节不同，不能直接推断算式错误或语义不同；仍需版本比较。
- mip0 用例包含旧绝对路径 D:/project/addrlib/ADDRLIB.md。当前路径已变，原文保留，通过仓库入口导航。
- 旧 README 链接的 ADDRLIB_comment.md 不存在；新入口已移除无效链接，历史声明保留在首次提交及旧分析中。
- upstream/ 与 verilog/ 目前仅有 README，没有完整上游源码或 RTL 模型。

## 使用原则

优先阅读主文档与任务对应的可信用例，用例按其明确的源版本和假设解释。不要将旧审计中的 GFX 代际判定、行号或“454 项通过”直接当成当前基准结论。

既有资料全部纳入版本控制，包括备份；本轮仅重写两个导航 README，补充管理文件。全仓库禁用 Git 换行自动转换以保留文档哈希。基准校验脚本检查字节一致性，不做技术正确性判断。

## 下一步建议

1. 如需继续技术分析，先处理 REVIEW_QUEUE.md 的源版本映射，明确当前主文档相对两个用例源版本的差异。
2. 复核 mip4、版本审计及其余分析，每项记录来源、输入、实际检查和结果。
3. 用户要求远程托管时，再配置目标远程并推送。

工作过程见 WORK_LOG.md；后续会话遵循根目录 AGENTS.md。
